package com.two.channelmyanmar.preference;

import android.content.Context;

public class PreferenceManager extends BasePreferenceManager {
    private static final String PREF_NAME = "toewaioo.dev";

    public PreferenceManager(Context context) {
        super(context, PREF_NAME);
    }

    // Add specific preference methods here if needed
}
